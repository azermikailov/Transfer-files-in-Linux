{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "g++ -g socket.cpp basenet.cpp server.cpp receiverworker.cpp -lstdc++fs main.cpp -o Server -pthread -std=c++17",
            "problemMatcher": [
                "$gcc"
            ]
        }
    ]
}